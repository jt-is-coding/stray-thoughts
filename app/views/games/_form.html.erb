<%= form_with(model: game) do |form| %>
  <% if game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, value: @title %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.text_field :image, value: @image_icon %>
  </div>

  <div>
    <%= form.label :platforms, style: "display: block" %>
    <%= form.text_field :platforms, value: @platforms_string %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description, value: @description %>
  </div>

  <div>
    <%= form.label :api_id, style: "display: block" %>
    <%= form.number_field :api_id, value: @api_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
