<%= form_with(model: game) do |form| %>
  <% if game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :title, value: @title %>
  </div>

  <div>
    <%= form.hidden_field :image, value: @image_icon %>
  </div>

  <div>
    <%= form.hidden_field :platforms, value: @platforms_string %>
  </div>

  <div>
    <%= form.hidden_field :description, value: @description %>
  </div>

  <div>
    <%= form.hidden_field :api_id, value: @api_id %>
  </div>

  <div>
    <%= form.submit "Create note" %>
  </div>
<% end %>
